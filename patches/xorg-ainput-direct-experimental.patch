diff --git a/dix/eventconvert.c b/dix/eventconvert.c
index 8e830c5..bbcca35 100644
--- a/dix/eventconvert.c
+++ b/dix/eventconvert.c
@@ -33,6 +33,7 @@
 #endif

 #include <stdint.h>
+#include <math.h>
 #include <X11/X.h>
 #include <X11/extensions/XIproto.h>
 #include <X11/extensions/XI2proto.h>
@@ -57,6 +58,8 @@ static int eventToKeyButtonPointer(DeviceEvent *ev, xEvent **xi, int *count);
 static int eventToDeviceChanged(DeviceChangedEvent *ev, xEvent **dcce);
 static int eventToDeviceEvent(DeviceEvent *ev, xEvent **xi);
 static int eventToRawEvent(RawDeviceEvent *ev, xEvent **xi);
+static int eventToAInputDeviceEvent(DeviceEvent *ev, xEvent **xi);
+static int eventToAInputRawEvent(RawDeviceEvent *ev, xEvent **xi);
 static int eventToBarrierEvent(BarrierEvent *ev, xEvent **xi);
 static int eventToTouchOwnershipEvent(TouchOwnershipEvent *ev, xEvent **xi);
 static int eventToGestureSwipeEvent(GestureEvent *ev, xEvent **xi);
@@ -278,6 +281,8 @@ EventToXI2(InternalEvent *ev, xEvent **xi)
     case ET_TouchBegin:
     case ET_TouchUpdate:
     case ET_TouchEnd:
+        if (ev->device_event.flags & AINPUT_FAST_EVENT)
+            return eventToAInputDeviceEvent(&ev->device_event, xi);
         return eventToDeviceEvent(&ev->device_event, xi);
     case ET_TouchOwnership:
         return eventToTouchOwnershipEvent(&ev->touch_ownership_event, xi);
@@ -295,6 +300,8 @@ EventToXI2(InternalEvent *ev, xEvent **xi)
     case ET_RawTouchBegin:
     case ET_RawTouchUpdate:
     case ET_RawTouchEnd:
+        if (ev->raw_event.flags & AINPUT_FAST_EVENT)
+            return eventToAInputRawEvent(&ev->raw_event, xi);
         return eventToRawEvent(&ev->raw_event, xi);
     case ET_BarrierHit:
     case ET_BarrierLeave:
@@ -746,6 +753,170 @@ eventToDeviceEvent(DeviceEvent *ev, xEvent **xi)
     return Success;
 }

+static int
+AInputDeviceEventValuatorCount(DeviceEvent *ev)
+{
+    if (ev->type != ET_Motion)
+        return 0;
+
+    if (!BitIsOn(ev->valuators.mask, 0) ||
+        !BitIsOn(ev->valuators.mask, 1))
+        return -1;
+
+    return 2;
+}
+
+static int
+AInputButtonMaskLen(DeviceEvent *ev)
+{
+    int i;
+
+    for (i = sizeof(ev->buttons) - 1; i >= 0; i--)
+        if (ev->buttons[i])
+            return bytes_to_int32(i + 1);
+
+    return 0;
+}
+
+static inline FP1616
+AInputDoubleToFP1616(double in)
+{
+    return (FP1616) (in * 65536.0);
+}
+
+static inline FP3232
+AInputDoubleToFP3232(double in)
+{
+    FP3232 ret;
+    int32_t integral;
+    double tmp = floor(in);
+
+    integral = (int32_t) tmp;
+    ret.integral = integral;
+    ret.frac = (uint32_t) ((in - integral) * (1ULL << 32));
+
+    return ret;
+}
+
+static int
+eventToAInputDeviceEvent(DeviceEvent *ev, xEvent **xi)
+{
+    int nvals = AInputDeviceEventValuatorCount(ev);
+    int btlen = AInputButtonMaskLen(ev);
+    int vallen = (nvals == 2) ? 1 : 0; /* Axes 0 and 1 fit in one word. */
+    int len;
+    xXIDeviceEvent *xde;
+    char *ptr;
+    FP3232 *axisval;
+
+    if (nvals < 0)
+        return eventToDeviceEvent(ev, xi);
+
+    len = sizeof(xXIDeviceEvent) + btlen * 4 + vallen * 4;
+    len += nvals * sizeof(FP3232);
+
+    *xi = malloc(len);
+    if (*xi == NULL)
+        return BadAlloc;
+
+    xde = (xXIDeviceEvent *) *xi;
+    memset(xde, 0, sizeof(*xde));
+    xde->type = GenericEvent;
+    xde->extension = IReqCode;
+    xde->evtype = GetXI2Type(ev->type);
+    xde->time = ev->time;
+    xde->length = bytes_to_int32(len - sizeof(xEvent));
+    xde->detail = ev->detail.button;
+    xde->root = ev->root;
+    xde->buttons_len = btlen;
+    xde->valuators_len = vallen;
+    xde->deviceid = ev->deviceid;
+    xde->sourceid = ev->sourceid;
+    xde->root_x = AInputDoubleToFP1616(ev->root_x + ev->root_x_frac);
+    xde->root_y = AInputDoubleToFP1616(ev->root_y + ev->root_y_frac);
+    xde->flags = ev->flags & ~AINPUT_FAST_EVENT;
+
+    if (ev->key_repeat)
+        xde->flags |= XIKeyRepeat;
+
+    xde->mods.base_mods = ev->mods.base;
+    xde->mods.latched_mods = ev->mods.latched;
+    xde->mods.locked_mods = ev->mods.locked;
+    xde->mods.effective_mods = ev->mods.effective;
+
+    xde->group.base_group = ev->group.base;
+    xde->group.latched_group = ev->group.latched;
+    xde->group.locked_group = ev->group.locked;
+    xde->group.effective_group = ev->group.effective;
+
+    ptr = (char *) &xde[1];
+    memset(ptr, 0, xde->buttons_len * 4 + xde->valuators_len * 4);
+    memcpy(ptr, ev->buttons, xde->buttons_len * 4);
+
+    if (nvals == 2) {
+        ptr += xde->buttons_len * 4;
+        axisval = (FP3232 *) (ptr + xde->valuators_len * 4);
+        SetBit(ptr, 0);
+        SetBit(ptr, 1);
+        axisval[0] = AInputDoubleToFP3232(ev->valuators.data[0]);
+        axisval[1] = AInputDoubleToFP3232(ev->valuators.data[1]);
+    }
+
+    return Success;
+}
+
+static int
+eventToAInputRawEvent(RawDeviceEvent *ev, xEvent **xi)
+{
+    int nvals = (ev->type == ET_RawMotion) ? 2 : 0;
+    int vallen = nvals ? 1 : 0;
+    int len;
+    xXIRawEvent *raw;
+    char *ptr;
+    FP3232 *axisval, *axisval_raw;
+
+    if (ev->type == ET_RawMotion &&
+        (!BitIsOn(ev->valuators.mask, 0) ||
+         !BitIsOn(ev->valuators.mask, 1)))
+        return eventToRawEvent(ev, xi);
+
+    len = sizeof(xXIRawEvent) + vallen * 4;
+    len += nvals * sizeof(FP3232) * 2;
+
+    *xi = malloc(len);
+    if (*xi == NULL)
+        return BadAlloc;
+
+    raw = (xXIRawEvent *) *xi;
+    memset(raw, 0, sizeof(*raw));
+    raw->type = GenericEvent;
+    raw->extension = IReqCode;
+    raw->evtype = GetXI2Type(ev->type);
+    raw->time = ev->time;
+    raw->length = bytes_to_int32(len - sizeof(xEvent));
+    raw->detail = ev->detail.button;
+    raw->deviceid = ev->deviceid;
+    raw->sourceid = ev->sourceid;
+    raw->valuators_len = vallen;
+    raw->flags = ev->flags & ~AINPUT_FAST_EVENT;
+
+    ptr = (char *) &raw[1];
+    memset(ptr, 0, raw->valuators_len * 4);
+
+    if (nvals == 2) {
+        axisval = (FP3232 *) (ptr + raw->valuators_len * 4);
+        axisval_raw = axisval + 2;
+        SetBit(ptr, 0);
+        SetBit(ptr, 1);
+        axisval[0] = AInputDoubleToFP3232(ev->valuators.data[0]);
+        axisval[1] = AInputDoubleToFP3232(ev->valuators.data[1]);
+        axisval_raw[0] = AInputDoubleToFP3232(ev->valuators.data_raw[0]);
+        axisval_raw[1] = AInputDoubleToFP3232(ev->valuators.data_raw[1]);
+    }
+
+    return Success;
+}
+
 static int
 eventToTouchOwnershipEvent(TouchOwnershipEvent *ev, xEvent **xi)
 {
diff --git a/dix/getevents.c b/dix/getevents.c
index 32bafe2..0bf0ac8 100644
--- a/dix/getevents.c
+++ b/dix/getevents.c
@@ -1292,6 +1292,160 @@ QueuePointerEvents(DeviceIntPtr device, int type,
     queueEventList(device, InputEventList, nevents);
 }

+/*
+ * Experimental AnbryInput-only relative 2D fast path.
+ */
+void
+QueueAInputRelativeMotion2DRawTimed(DeviceIntPtr device, double dx, double dy,
+                                    double raw_dx, double raw_dy, CARD32 ms)
+{
+    InternalEvent *events = InputEventList;
+    InternalEvent *out;
+    RawDeviceEvent *raw;
+    DeviceEvent *event;
+    DeviceIntPtr master;
+    ScreenPtr scr;
+    ValuatorMask history_mask = {0};
+    int prefix_events;
+    int num_events = 0;
+    int barrier_events = 0;
+    double old_x, old_y;
+    double screenx, screeny;
+
+    if (!device || !device->enabled || !device->valuator ||
+        device->valuator->numAxes < 2)
+        return;
+
+    scr = miPointerGetScreen(device);
+    if (!scr)
+        return;
+
+    master = (!IsMaster(device) && !IsFloating(device)) ?
+             GetMaster(device, MASTER_POINTER) : NULL;
+    if (master && master->last.slave != device)
+        out = UpdateFromMaster(events, device, DEVCHANGE_POINTER_EVENT, &num_events);
+    else
+        out = events;
+    prefix_events = num_events;
+
+    raw = &out->raw_event;
+    init_raw(device, raw, ms, MotionNotify, 0);
+    SetBit(raw->valuators.mask, 0);
+    SetBit(raw->valuators.mask, 1);
+    raw->valuators.data_raw[0] = raw_dx;
+    raw->valuators.data_raw[1] = raw_dy;
+    raw->valuators.data[0] = raw_dx;
+    raw->valuators.data[1] = raw_dy;
+    /* Use the server's standard XI2 wire conversion for client compatibility. */
+    raw->flags = 0;
+    out++;
+    num_events++;
+
+    old_x = device->last.valuators[0];
+    old_y = device->last.valuators[1];
+    screenx = old_x + dx;
+    screeny = old_y + dy;
+
+    scr = miPointerSetPosition(device, Relative, &screenx, &screeny,
+                               &barrier_events, out);
+    out += barrier_events;
+    num_events += barrier_events;
+
+    history_mask.last_bit = 1;
+    history_mask.mask[0] = 0x3;
+    history_mask.valuators[0] = screenx - scr->x;
+    history_mask.valuators[1] = screeny - scr->y;
+    updateHistory(device, &history_mask, ms);
+
+    device->last.valuators[0] = screenx;
+    device->last.valuators[1] = screeny;
+
+    if (master) {
+        master->last.valuators[0] = screenx;
+        master->last.valuators[1] = screeny;
+    }
+
+    event = &out->device_event;
+    init_device_event(event, device, ms, EVENT_SOURCE_NORMAL);
+    event->type = ET_Motion;
+    event->detail.button = 0;
+    event_set_root_coordinates(event, screenx - scr->x, screeny - scr->y);
+    SetBit(event->valuators.mask, 0);
+    SetBit(event->valuators.mask, 1);
+    event->valuators.data[0] = screenx - scr->x;
+    event->valuators.data[1] = screeny - scr->y;
+    event->flags = 0;
+    out++;
+    num_events++;
+
+    if (prefix_events == 0 && barrier_events == 0)
+        mieqEnqueueAInputMotionPair(device, &InputEventList[0],
+                                    &InputEventList[1]);
+    else
+        queueEventList(device, InputEventList, num_events);
+}
+
+/*
+ * Experimental AnbryInput-only mouse button fast path.
+ */
+void
+QueueAInputButton(DeviceIntPtr device, int button, int is_down)
+{
+    CARD32 ms = GetTimeInMillis();
+    InternalEvent *events = InputEventList;
+    InternalEvent *out;
+    RawDeviceEvent *raw;
+    DeviceEvent *event;
+    ScreenPtr scr;
+    int prefix_events;
+    int num_events = 0;
+    int type;
+
+    if (!device || !device->enabled || !device->button || !button ||
+        button >= MAX_BUTTONS)
+        return;
+
+    scr = miPointerGetScreen(device);
+    if (!scr)
+        return;
+
+    type = is_down ? ButtonPress : ButtonRelease;
+    if (!!is_down == !!button_is_down(device, button, BUTTON_POSTED))
+        return;
+
+    out = UpdateFromMaster(events, device, DEVCHANGE_POINTER_EVENT, &num_events);
+    prefix_events = num_events;
+
+    raw = &out->raw_event;
+    init_raw(device, raw, ms, type, button);
+    raw->flags = 0;
+    out++;
+    num_events++;
+
+    event = &out->device_event;
+    init_device_event(event, device, ms, EVENT_SOURCE_NORMAL);
+    event->type = is_down ? ET_ButtonPress : ET_ButtonRelease;
+    event->detail.button = button;
+    event->flags = 0;
+    event_set_root_coordinates(event,
+                               device->last.valuators[0] - scr->x,
+                               device->last.valuators[1] - scr->y);
+
+    if (is_down)
+        set_button_down(device, button, BUTTON_POSTED);
+    else
+        set_button_up(device, button, BUTTON_POSTED);
+
+    out++;
+    num_events++;
+
+    if (prefix_events == 0)
+        mieqEnqueueAInputButtonPair(device, &InputEventList[0],
+                                    &InputEventList[1]);
+    else
+        queueEventList(device, InputEventList, num_events);
+}
+
 /**
  * Helper function for GetPointerEvents, which only generates motion and
  * raw motion events for the slave device: does not update the master device.
diff --git a/include/eventstr.h b/include/eventstr.h
index a9926ea..a23285f 100644
--- a/include/eventstr.h
+++ b/include/eventstr.h
@@ -92,6 +92,9 @@ enum DeviceEventSource {
   EVENT_SOURCE_FOCUS, /**< Keys or buttons previously down on focus-in */
 };

+/* Internal-only marker for AnbryInput experimental fast-path events. */
+#define AINPUT_FAST_EVENT (1u << 31)
+
 /**
  * Used for ALL input device events internal in the server until
  * copied into the matching protocol event.
diff --git a/include/input.h b/include/input.h
index cdb5d5a..78f30dd 100644
--- a/include/input.h
+++ b/include/input.h
@@ -476,6 +476,14 @@ extern _X_EXPORT void QueuePointerEvents(DeviceIntPtr pDev,
                                          int buttons,
                                          int flags, const ValuatorMask *mask);

+extern _X_EXPORT void QueueAInputRelativeMotion2DRawTimed(DeviceIntPtr pDev,
+                                                          double dx, double dy,
+                                                          double raw_dx, double raw_dy,
+                                                          CARD32 time_ms);
+
+extern _X_EXPORT void QueueAInputButton(DeviceIntPtr pDev,
+                                        int button, int is_down);
+
 extern _X_EXPORT int GetKeyboardEvents(InternalEvent *events,
                                        DeviceIntPtr pDev,
                                        int type,
diff --git a/mi/mi.h b/mi/mi.h
index 8b0a231..29bcf81 100644
--- a/mi/mi.h
+++ b/mi/mi.h
@@ -192,6 +192,13 @@ extern _X_EXPORT void mieqEnqueue(DeviceIntPtr /*pDev */ ,
                                   InternalEvent *       /*e */
     );

+extern _X_EXPORT void mieqEnqueueAInputMotionPair(DeviceIntPtr /*pDev */,
+                                                  InternalEvent * /*raw */,
+                                                  InternalEvent * /*motion */);
+extern _X_EXPORT void mieqEnqueueAInputButtonPair(DeviceIntPtr /*pDev */,
+                                                  InternalEvent * /*raw */,
+                                                  InternalEvent * /*button */);
+
 extern _X_EXPORT void mieqSwitchScreen(DeviceIntPtr /* pDev */ ,
                                        ScreenPtr /*pScreen */ ,
                                        Bool     /*set_dequeue_screen */
diff --git a/mi/mieq.c b/mi/mieq.c
index c98d468..617028f 100644
--- a/mi/mieq.c
+++ b/mi/mieq.c
@@ -267,6 +267,80 @@ mieqEnqueue(DeviceIntPtr pDev, InternalEvent *e)
     miEventQueue.tail = (oldtail + 1) % miEventQueue.nevents;
 }

+static inline void
+mieqEnqueueAInputPair(DeviceIntPtr pDev, InternalEvent *first,
+                      InternalEvent *second, int second_is_motion)
+{
+    InternalEvent *first_out;
+    InternalEvent *second_out;
+    ScreenPtr screen;
+    unsigned int first_tail;
+    unsigned int second_tail;
+    size_t n_enqueued;
+    Time time;
+
+    if (!first || !second ||
+        first->any.length == 0 || first->any.length > sizeof(InternalEvent) ||
+        second->any.length == 0 || second->any.length > sizeof(InternalEvent) ||
+        !((first->any.type == ET_RawMotion &&
+           second->any.type == ET_Motion) ||
+          (first->any.type == ET_RawButtonPress &&
+           second->any.type == ET_ButtonPress) ||
+          (first->any.type == ET_RawButtonRelease &&
+           second->any.type == ET_ButtonRelease))) {
+        miEventQueue.dropped++;
+        return;
+    }
+
+    n_enqueued = mieqNumEnqueued(&miEventQueue);
+    if (n_enqueued + 2 >= miEventQueue.nevents) {
+        if (!mieqGrowQueue(&miEventQueue, miEventQueue.nevents << 1)) {
+            miEventQueue.dropped++;
+            return;
+        }
+    }
+
+    time = first->any.time;
+    if (time < miEventQueue.lastEventTime &&
+        miEventQueue.lastEventTime - time < 10000)
+        time = miEventQueue.lastEventTime;
+
+    screen = pDev ? EnqueueScreen(pDev) : NULL;
+    first_tail = miEventQueue.tail;
+    second_tail = (first_tail + 1) % miEventQueue.nevents;
+    first_out = miEventQueue.events[first_tail].events;
+    second_out = miEventQueue.events[second_tail].events;
+
+    memcpy(first_out, first, first->any.length);
+    memcpy(second_out, second, second->any.length);
+    first_out->any.time = time;
+    second_out->any.time = time;
+
+    miEventQueue.events[first_tail].pScreen = screen;
+    miEventQueue.events[first_tail].pDev = pDev;
+    miEventQueue.events[second_tail].pScreen = screen;
+    miEventQueue.events[second_tail].pDev = pDev;
+    miEventQueue.lastEventTime = time;
+    miEventQueue.lastMotion = second_is_motion;
+    miEventQueue.tail = (second_tail + 1) % miEventQueue.nevents;
+}
+
+void
+mieqEnqueueAInputMotionPair(DeviceIntPtr pDev,
+                            InternalEvent *raw,
+                            InternalEvent *motion)
+{
+    mieqEnqueueAInputPair(pDev, raw, motion, pDev ? pDev->id : 0);
+}
+
+void
+mieqEnqueueAInputButtonPair(DeviceIntPtr pDev,
+                            InternalEvent *raw,
+                            InternalEvent *button)
+{
+    mieqEnqueueAInputPair(pDev, raw, button, 0);
+}
+
 /**
  * Changes the screen reference events are being enqueued from.
  * Input events are enqueued with a screen reference and dequeued and
